'use client'

import { Button } from '@mui/material'

const ProductCard = () => {
  return (
    <div>
      <Button variant='contained' color='primary'>
        Add to Cart
      </Button>
    </div>
  )
}

export default ProductCard
